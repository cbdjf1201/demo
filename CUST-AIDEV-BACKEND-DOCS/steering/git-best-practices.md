# Git Best Practices / Git 最佳实践

- 提交粒度保持单一关注点，不要把模型、前端、文档杂糅在一个 commit 中
- 分支命名应体现 `CR` 或 `Task ID`
- 自动生成内容与手工修改内容不要无理由混在一起
- 当需求范围或实现路径发生变化时，必须同步更新 `task` 与 `change-log`
- 面试演示项目也要保持历史清晰，避免一串没有语义的 fixup 提交
